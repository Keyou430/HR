# Development & testing dependencies
# Install: pip install -r requirements-dev.txt
# Or:      pip install -e ".[dev]"

pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.24
ruff>=0.9.0
bandit>=1.8.0
coverage>=7.0
